.row
  .col-md-6.col-md-offset-3
    %fieldset
      %legend Update Grant Application
      = render(:partial => 'shared/form_errors', :locals => {:obj => @funding_bucket}) unless @funding_bucket.errors.empty?

      = render 'app_form'