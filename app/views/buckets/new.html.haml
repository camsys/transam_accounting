.row
  .col-md-6.col-md-offset-3
    %fieldset
      %legend Create Bucket
      -#= render(:partial => 'shared/form_errors', :locals => {:obj => @bucket}) unless @bucket.errors.empty?

      = render 'form'