.row
  .col-md-6.col-md-offset-3
    %fieldset
      %legend Update Expenditure

      = render(:partial => 'shared/form_errors', :locals => {:obj => @expenditure}) unless @expenditure.errors.empty?

      = render 'form'

      = render 'scripts'
