.row
  .col-md-6.col-md-offset-3
    %fieldset
      %legend Update General Ledger Account
      = render(:partial => 'shared/form_errors', :locals => {:obj => @ledger_account}) unless @ledger_account.errors.empty?

      = render 'form'

= render 'scripts'
