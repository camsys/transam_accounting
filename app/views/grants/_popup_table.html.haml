- # This parital does not work properly.  We will leave it out until it functions properly.


- # %i.fa.fa-info-circle.text-info.info-popup{:data => {:title => "#{grant.grant_number}: CapEx", :url => summary_info_grant_path(grant, :format => "js"), :container => 'body', :html => 'true', :placement => 'left', :animation => 'true'}}
